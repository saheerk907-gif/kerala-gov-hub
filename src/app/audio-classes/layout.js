import { buildMetadata } from '@/lib/seo';

export const metadata = buildMetadata({
  title: 'Audio Classes — Kerala Service Rules (KSR) Awareness',
  description: 'Listen to all KSR awareness audio classes for Kerala government employees. Learn service rules, pension, leave, and pay rules through audio episodes.',
  path: '/audio-classes',
  keywords: ['KSR audio classes', 'Kerala service rules audio', 'government employee awareness classes', 'KSR Malayalam audio'],
});

export default function Layout({ children }) { return children; }
